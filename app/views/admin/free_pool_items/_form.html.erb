<%= form_for([:admin, @free_pool_item]) do |f| %>
  <% if @free_pool_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@free_pool_item.errors.count, "error") %> prohibited this free pool item from being saved:</h2>

      <ul>
      <% @free_pool_item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :problem_id %><br>
    <%= f.collection_select(:problem_id, Problem.all, :id, :name, :prompt => 'Select') %>
  </div>
  <br>

  <div class="form-group">
    <%= f.submit class: 'btn btn-success' %>
  </div>
<% end %>
